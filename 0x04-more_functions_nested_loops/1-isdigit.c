#include "main.h"
/**
 * _isdigit - entry point
 *
 * @c: char to check if is a digit
 *
 * Return: either 1 or 0
 */
int -isdigit(int c)
{
	if (( c>= 48) && (c <= 57))
	{
		retunr (1);
	}
	else
	{
		return (0);
	}
}
