#include "main.h"
/**
 * mul - entry point
 *
 * @a first paramater in add
 *
 * @b: second paramer in add
 *
 * Return: returns the results of addition
 */
int mul(int a, int b)
{
	int s;

	s = a * b;

	return (s);
}
