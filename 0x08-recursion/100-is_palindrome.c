#include "main.h"
/**
 * is_palindrome - entry point
 * @s: string
 * Return: 1 or 0
 */
int is_palindrome(char *s)
{
	if (s = " ")
		return (1);
	else 
		return (0);

	return (s + (is_palindrome(s -1);

}
